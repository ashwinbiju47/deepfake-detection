# Deepfake Detection Platform - Backend dependencies (pinned)
# Core web framework + API
Django==5.0.6
djangorestframework==3.15.1

# Realtime / ASGI (Django Channels + Redis channel layer)
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.2

# Static / single-origin SPA serving (used in the deployed container)
whitenoise==6.7.0

# Async task queue + broker
celery==5.4.0
redis==5.0.4

# Persistence (PostgreSQL driver for the Django ORM)
psycopg[binary]

# Configuration
python-dotenv==1.0.1

# ML inference
torch
torchvision

# Media processing
opencv-python==4.9.0.80
ffmpeg-python==0.2.0
librosa==0.10.2
mtcnn==0.1.1
mediapipe

# Explainability / reporting
reportlab==4.2.0
weasyprint==62.1
# Evaluation figures (confusion matrices, ROC / precision-recall curves and the
# fusion-weight ablation chart). pdf/agg backends only - no GUI toolkit needed.
matplotlib==3.9.4

# Testing (unit + property-based)
pytest==8.2.1
pytest-django==4.8.0
hypothesis==6.103.0
